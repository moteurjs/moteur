{
    "name": "@webng/gfx",
    "main": "dist",
    "scripts": {
        "build": "tsc",
        "build-engine": "tsc",
        "dev": "tsc -w",
        "clean": "rm -rf ./dist"
    },
    "dependencies": {
        "@webng/ecs": "workspace:*",
        "@webng/math": "workspace:*"
    },
    "devDependencies": {
        "@types/web": "^0.0.114"
    }
}
