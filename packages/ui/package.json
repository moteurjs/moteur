{
    "name": "@webng/ui",
    "main": "dist",
    "scripts": {
        "build": "tsc",
        "build-engine": "tsc",
        "dev": "tsc -w",
        "clean": "rm -rf ./dist"
    },
    "dependencies": {
        "@webng/ecs": "workspace:*",
        "@webng/event": "workspace:*"
    }
}
