{
    "name": "@moteur/event",
    "main": "dist",
    "scripts": {
        "build": "tsc",
        "build-moteur": "pnpm build",
        "dev": "tsc -w",
        "clean": "rm -rf ./dist"
    },
    "dependencies": {
        "@moteur/ecs": "workspace:*"
    },
    "devDependencies": {
        "@types/web": "^0.0.114"
    }
}
